# local-only: working docs and tooling config, not part of the deliverable
start_docs/
.claude/

# secrets
.env

# data (bind mounts, not docker volumes — prune won't touch them)
data/
checkpoints/
backups/

docs/

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# os / editor
.DS_Store

# local agent context — personal tooling, not part of the deliverable
CLAUDE.md
.playwright-mcp/

# terraform working dirs — providers are binaries; lockfile IS committed
infra/terraform/**/.terraform/
infra/terraform/**/tfplan
infra/terraform/tfplan
.terraform/


/portfolio-assets
.vercel
